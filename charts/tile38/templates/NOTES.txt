{{- $fullName := include "tile38.fullname" . -}}
{{- $namespace := .Release.Namespace -}}
{{- $leaderServicePort := .Values.leader.service.tilePort -}}
{{- $followerServicePort := .Values.followers.service.tilePort -}}


Tile38 Leader can be accessed within the cluster on port {{ $leaderServicePort }} at {{ $fullName }}-leader.{{ $namespace }}.svc.cluster.local
Followers are accessible on port {{ $followerServicePort }} at {{ $fullName }}-followers.{{ $namespace }}.svc.cluster.local

To connect to Tile38 from outside the cluster, you can use port-forwarding:
  kubectl port-forward --namespace {{ $namespace }} svc/{{ $fullName }}-leader {{ $leaderServicePort }}:{{ $leaderServicePort }}

Then you can connect to Tile38 at localhost:{{ $leaderServicePort }}
