{{- if and .Values.followers.enabled .Values.followers.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tile38.fullname" . }}-followers-config
  labels:
    {{- include "tile38.componentLabels" (dict "ctx" . "component" "follower") | nindent 4 }}
data:
  config: |-
{{- include "tile38.follower.configJson" . | nindent 4 }}
{{- end }}
