apiVersion: v1
kind: Service
metadata:
  name: {{ include "tile38.fullname" . }}-leader
  labels:
    {{- include "tile38.componentLabels" (dict "ctx" . "component" "leader") | nindent 4 }}
    {{- with .Values.leader.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.leader.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.leader.service.type }}
  selector:
    {{- include "tile38.componentSelectorLabels" (dict "ctx" . "component" "leader") | nindent 4 }}
  ports:
    - name: tile
      port: {{ .Values.leader.service.tilePort }}
      targetPort: tile
      protocol: TCP
    - name: monitoring
      port: {{ .Values.leader.service.monitoringPort }}
      targetPort: monitoring
      protocol: TCP
